extends layout

block style
  link(rel = "stylesheet" href = "/style/dist/vote.css")
block script
  script(type = "text/javascript" src = "/js/dist/vote.js")

block content
  #vote.container-fluid
    .content
      .row
        .col-md-12.text-center
          h2= vote.question
      form
        .row
          .col-sm-12
            ul#answer_list
              for answer in vote.answer.split("\n")
                .form-group
                  li.form-control
                    input#answer1(type='checkbox' value=answer)
                    | &nbsp;&nbsp;
                    span= answer
        .row
          .col-sm-4.col-sm-offset-4
            .form-group
              button#voteOkBtn.btn.btn-block(onclick='voteOk()') 투표하기
          if user.u_id === vote.u_id
            .col-sm-4
              .form-group.text-right
                a(href='/vote/update/'+vote.id) 수정하기